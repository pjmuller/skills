[project]
name = "agent-skills-tests"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
  "google-auth>=2",
  "google-auth-oauthlib>=1",
  "httpx>=0.27",
  "python-dotenv>=1",
  "requests>=2.32,<3",
]
